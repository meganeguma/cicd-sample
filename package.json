{
  "name": "cicd-sample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "eslint \"src/**/*.js\"",
    "test": "node --test --experimental-test-coverage",
    "coverage": "c8 --reporter=html npm run test",
    "docs": "node scripts/generate-docs.mjs",
    "build": "cp -R src public && cp -R docs public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.56.0",
    "prettier": "^3.0.0",
    "c8": "^8.0.1"
  },
  "type": "module",
  "engines": {
    "node": ">=18"
  }
}
